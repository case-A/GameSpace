var reviewStorage = [];

function Review(gameplayScore, visualsScore, storyScore, comments, overallScore){
    this.gameplayScore = gameplayScore,
    this.visualsScore = visualsScore,
    this.storyScore = storyScore,
    this.comments = comments,
    this.overallScore = overallScore
}


//It should have a function to add gameplayScore property value (1-5)
//It should have a function to add visualScore property value (1-5)
//It should have a function to add storyScore property value (1-5)
//It should have a function to add comments


//It should have a function that totals and averages the 'Scores'